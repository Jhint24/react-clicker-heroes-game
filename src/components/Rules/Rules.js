import React from 'react';
import './Rules.css';

const Rules = () => (
  <div className="banner text-center .d-md-flex align-items-center justify-content-center">
    <p className="m-0">
      Each image clicked earns you points, but if you click on the same image twice, Game Over!
    </p>
  </div>
);
export default Rules;
